// Keep in sync with WOM_GROUP_ID / GROUP_ID in vite.config.ts, api/wom-proxy.ts,
// and api/runeprofile-proxy.ts — those live outside src/'s bundling boundary so
// they can't import this constant directly.
export const WOM_GROUP_ID = 22206;
